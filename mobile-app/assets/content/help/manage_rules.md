Browse and edit every spam rule the scanner will apply. Each rule has an execution order, one or more condition buckets (from / header / subject / body), and an action (delete / move / categorize). Use the filter bar to narrow by pattern category or subtype.

Patterns that match ReDoS heuristics (catastrophic-backtracking regex) are rejected when you save; this prevents scanner hangs. Rewrite the pattern or simplify its quantifiers to save.
