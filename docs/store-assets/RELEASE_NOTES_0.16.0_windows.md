# Release notes -- 0.16.0 -- Microsoft Store

**PROVISIONAL.** Written at the Phase 3.7.0b version bump. **RE-DERIVE at Phase 7.7 from the
finished CHANGELOG** (Sprint 69 IMP-5).

**Derived from** `CHANGELOG.md` `[Unreleased]`, Sprint 73 entries, per ADR-0043. Per-store by
design: Windows sees only what applies to Windows. The Android background-scheduling work is
excluded because Windows has no Doze equivalent.

**Not yet submitted.**

---

You can cancel a scan that is taking too long, from the screen you are already on.

A manual scan is no longer refused because of a background scan that has already finished.

When an account is set to read-only, the app now shows what it would have filed, so you can check
a new rule before letting it act.

The app version is visible on every screen.
